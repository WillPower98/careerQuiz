<!DOCTYPE html>
<html>

<head>
  <title>Dept of State</title>
  <link rel="stylesheet" type="text/css" href="styles.css">
</head>

<body>

  <img src="<%=quizSession._currentQuestion.image%>"style="width:100%">
  <h2><%= quizSession._currentQuestion.title %></h2>

  <form class="" action="/" method="post">
    <%  quizSession._currentQuestion.choices.forEach((choice) => { %>
      <label for="q1" onclick="getCareers()"><input type="radio" name="userChoice" value="<%=choice%>"><%=choice%></label>
    <%  }) %>
    <input type="hidden" name="quizSessionId" value="<%=quizSession._sessionId%>">
    <input type="hidden" name="questionTitle" value="<%=quizSession._currentQuestion.title%>"></input>
    <button type="submit" name="button">Submit Answer</button>
  </form>

</body>
